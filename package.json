{
  "name": "vokabeln",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
